"use strict";

const divAccion = document.querySelector("divAcción");


document.querySelector("tablaColores").addEventListener("click", e=>{
    if(e.target.tagName != "DIV" && e.target.tagName != "p") return;

    if(e.target.id == "tablaColores") return;

    if(e.target.tagName == "p"){
        
    }

});